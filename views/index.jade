extends layout

block content
    //- include ./includes/header.jade
    .blogs.box
        h4.center-header
        //- Enter a search bar here instead
        h3 Recent Posts
            ul
                //- consider a two column table of blog posts too?
                each r, i in rows
                    case r.topic
                        when "azure"
                            p.tag.azure
                        when "chrome"
                            p.tag.chrome
                        when "dotnet"
                            p.tag.dotnet
                        when "nodejs"
                            p.tag.nodejs
                        when "javascript"
                            p.tag.javascript
                        default
                            p.tag.default
                    li
                        a(href="/posts/#{r.pid}")
                            span #{r.title}
                        p.preview #{r.body_preview}
