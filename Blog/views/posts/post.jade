extends ../layout

block content
    .box
        h1 Here is post info
        ul
            li= row.title
            unless row.thumbnail == ""
                li= row.thumbnail
            li= row.tags
            li= row.topic
            li= row.body_preview
            
            //- Escape incoming HTML
            li!= row.body_markdown

